
javascript const blocks = document.querySelectorAll('.block'); blocks.forEach(block => { block.addEventListener('dragstart', dragStart); }); function dragStart(e) { e.dataTransfer.setData('text/plain', e.target.id); } const workspace = document.getElementById('workspace'); workspace.addEventListener('dragover', (e) => e.preventDefault()); workspace.addEventListener('drop', drop); function drop(e) { const id = e.dataTransfer.getData('text/plain'); const block = document.getElementById(id); const newBlock = block.cloneNode(true); newBlock.style.position = 'absolute'; newBlock.style.left = `${e.clientX}px`; newBlock.style.top = `${e.clientY}px`; workspace.appendChild(newBlock); }
